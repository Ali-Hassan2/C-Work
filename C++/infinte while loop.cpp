//as the for loop can be infinite so while loop can also be infinite for this
//we need to write as
#include<iostream>
using namespace std;
int main()
    {
        int a=1;
      while(true)
      // as it alwqays remains true so
      {
        cout<<a<<endl;
        // now as by increment add increment
        a++;
      }
      return 0;
    }